import type { NextPage } from "next";
import { Button, useToast } from "@chakra-ui/react";

const Add: NextPage = () => {
    return (
        <div>
            a
        </div>
      );
}

export default Add;